export default class SinglePost {
    constructor(title, body, authorName, authorId, authorPosts) {
        this.title = title
        this.body = body
        this.authorName = authorName
        this.authorId = authorId
        this.authorPosts = authorPosts
    }
}